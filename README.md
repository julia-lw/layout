# layout
# Para instalar o programa: npx -p @angular/cli ng new (nome do projeto) --style=css --routing=true --ssr=false
# Para acessar a página: npx ng serve
# Para entrar na pasta: cd .\nome do projeto\
# Para criar novo componente: npx ng g c (nome do componente). E/ou: ng g c (nome do componente)
# Para consultar comandos: npx ng --help
# Para baixar os pacotes: npx npm install